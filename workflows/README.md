# Workflow exports

These are sanitized public exports. Credential references use placeholder IDs. After import, re-select the matching n8n credentials in HTTP/SMTP nodes before publishing.

`all_workflows.json` contains the same six workflows in one import bundle.
